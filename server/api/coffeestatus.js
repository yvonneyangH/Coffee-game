const express = require('express');
const app = express.Router();
const {User,CoffeeStatus,ProducerList} = require('../db');

// app.post('/',async(req,res,next) => {
//     try{
//         const coffee_status = await CoffeeStatus.find
//     }
    
// })